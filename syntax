main:
    run test(rax, rbx)
    run test(rbx, rax)

decl test(arg1, arg2):
    mov arg1, arg2
    return

=>

main:
    push rax
    push rbx
    call test
    
    push rbx
    push rax
    call test

test:
    pop rsi
    pop rdi
    mov rdi, rsi
    ret 



> find "decl"
> read until "\n"
> change contents of function
> find "run" 